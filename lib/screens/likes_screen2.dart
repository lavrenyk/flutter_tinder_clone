import 'package:flutter/material.dart';

class LikesScreen2 extends StatelessWidget {
  const LikesScreen2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
